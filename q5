from numbers import Number

def check_divisibility(num, divisor):
    """
    Task 1
    - Create a function to check if the number (num) is divisible by another number (divisor).
    - Both num and divisor must be numeric.
    - Return True if num is divisible by divisor, False otherwise.
    """
    # validate numeric (exclude bool)
    if not (isinstance(num, Number) and isinstance(divisor, Number)) or isinstance(num, bool) or isinstance(divisor, bool):
        return -1  # safe handling if wrong input type

    if divisor == 0:
        return False

    return num % divisor == 0


# Task 2
# Invoke the function "check_divisibility" using the following scenarios:

print(check_divisibility(10, 2))  # expected: True
print(check_divisibility(7, 3))   # expected: False
